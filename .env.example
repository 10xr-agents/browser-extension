DEBUG_MODE=false

# Backend API Base URL
# This is the base URL for the backend API server
# For local development, use: http://localhost:3000
# For production, use your production API URL
# Note: WEBPACK_ prefix is required for webpack's built-in dotenv (5.103.0+)
# Only variables with this prefix are exposed to the bundle for security
WEBPACK_API_BASE=http://localhost:3000
